// Placeholder — voyant-cloud's deploy pipeline replaces this file
// with the tenant's real `voyant workflows build` output before
// running `wrangler deploy`. Having a file here at all lets the
// Docker build succeed for dry-runs and local development.
