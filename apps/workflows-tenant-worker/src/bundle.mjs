// Placeholder — voyant-cloud's deploy pipeline replaces this file
// with the tenant's compiled `voyant workflows build` output before
// `wrangler deploy`. Having a file here lets `wrangler deploy --dry-run`
// succeed locally.
